# Example environment variables for the dahoo project
# Copy this file to `.env` and fill the values before running the app.

# Django
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (Postgres)
DB_NAME=dahooestate_db
DB_USER=postgres
DB_PASSWORD=admin
DB_HOST=localhost
DB_PORT=5432

# Email (optional)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Third-party keys (optional)
SENTRY_DSN=
